"use client";import{useState}from"react";export default function BMI(){const[h,setH]=useState("");const[w,setW]=useState("");const bmi=h&&w?(Number(w)/((Number(h)/100)**2)).toFixed(1):"";return <main className="page"><a className="back" href="/">← HOME</a><p className="eyebrow">IRONFORGE / TOOL</p><h1>BMI <i>CHECK.</i></h1><p className="pageLead">A general screening calculation—not a medical diagnosis.</p><div className="contactForm"><input type="number" min="1" placeholder="HEIGHT (CM)" value={h} onChange={e=>setH(e.target.value)}/><input type="number" min="1" placeholder="WEIGHT (KG)" value={w} onChange={e=>setW(e.target.value)}/>{bmi&&<div className="notice"><b>{bmi}</b><br/>{Number(bmi)<18.5?"Below the general reference range":Number(bmi)<25?"Within the general reference range":Number(bmi)<30?"Above the general reference range":"High BMI range"}</div>}</div></main>}
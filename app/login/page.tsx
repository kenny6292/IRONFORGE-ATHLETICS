"use client";import{FormEvent,useState}from"react";export default function Login(){const[submitted,setSubmitted]=useState(false);const submit=(e:FormEvent)=>{e.preventDefault();setSubmitted(true)};return <main className="page"><a className="back" href="/">← HOME</a><p className="eyebrow">IRONFORGE / MEMBER AREA</p><h1>MEMBER <i>LOGIN.</i></h1><p className="pageLead">Secure member access will connect to Supabase authentication in the production backend.</p><form className="contactForm" onSubmit={submit}><input required type="email" placeholder="EMAIL"/><input required type="password" placeholder="PASSWORD"/><button className="btn primary">SIGN IN →</button>{submitted&&<div className="notice">Authentication backend configuration is required before live sign-in.</div>}</form></main>}
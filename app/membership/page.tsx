"use client";import {useState} from "react";export default function Membership(){const [selected,setSelected]=useState("");const plans=[["FORGE","₦35,000","Gym access, group classes and member app"],["ELITE","₦60,000","Full access, all classes and recovery"],["PERFORMANCE","₦120,000","Elite access, 4 PT sessions and priority booking"]];return <main className="page"><a className="back" href="/">← HOME</a><p className="eyebrow">IRONFORGE / MEMBERSHIP</p><h1>CHOOSE YOUR <i>FORGE.</i></h1><p className="pageLead">Select a membership plan. Payment integration can be connected through Paystack or Flutterwave once production credentials are configured.</p><section className="plans">{plans.map(p=><article className="plan" key={p[0]}><span>{p[0]}</span><strong>{p[1]}<small>/ MONTH</small></strong><p>{p[2]}</p><button className="btn primary" onClick={()=>setSelected(p[0])}>SELECT {p[0]}</button></article>)}</section>{selected&&<div className="notice">Selected: <b>{selected}</b>. Checkout flow is ready for payment-provider configuration.</div>}</main>}